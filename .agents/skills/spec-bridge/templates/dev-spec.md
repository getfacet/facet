# Dev Spec: [Feature Name]

> Generated by `/spec-bridge`. Human approval spec. Mirror of
> `.agents/work/<slug>/execution.yaml` (WU ids/files/deps/checks must match).

## Overview
- Feature slug: `<slug>`
- Intake brief: `.agents/work/<slug>/intake.md`
- Context evidence: `.agents/work/<slug>/context.md`
- Affected `@facet/*` packages: [list]
- One-paragraph technical approach:

## Done-Criteria Mapping (DC → implementation)

| DC id | Statement | File / function | Test target | WU |
|---|---|---|---|---|
| DC-001 | [from brief] | [path:line] | [test file] | WU-1 |

## Invariant Fit Audit (Facet — REQUIRED)

For every invariant the brief marked `TOUCHES`, the concrete design that keeps it
safe. Mark others `N/A`.

| # | Invariant | Touched? | Safe design |
|---|---|---|---|
| 1 | Responsibility boundary (no backend/domain in Facet) | | |
| 2 | Mechanism vs policy | | |
| 3 | Fail-safe (never throw/inject; degrade to plain) | | |
| 4 | Declarative + tokens only | | |
| 5 | Flow-only safety (overlay = dedicated brick) | | |
| 6 | Two-writers coherence (ordering/version rule) | | |
| 7 | Backend via agent tools, never client fetch | | |

## Fail-safe & Boundary Checklist
- Malformed / empty / null input: [behavior]
- Deeply nested / cyclic tree: [behavior]
- Offline agent / rapid events / racing agent turn: [behavior]

## Risk Register (resolves every RISK-* from Stage 0)

| Risk id | Detected (file:line) | Resolution / waiver |
|---|---|---|
| RISK-INV-1 | | |
| RISK-API-1 | | |
| RISK-PKG-1 | | |
| RISK-SHAPE-1 | | |

## Public API Impact (published `@facet/*` surface)

| Package | Change | Additive/Breaking | Consumer migration |
|---|---|---|---|

## Module Shape & Scaffold Plan

Use this section to prevent accidental large-file growth and arbitrary
extractions before implementation starts. Do not require splitting by line count
alone; require evidence that the planned shape reduces drift, clarifies
ownership, preserves package boundaries, or improves testability.

| Area / file cluster | Current shape evidence | Planned shape | Public vs private | Import direction | Test placement | Rationale / do-not-split note |
|---|---|---|---|---|---|---|
| `packages/...` | [file:line, line count, or scaffold evidence] | [preserve / sibling-helper / role-directory / package-shared / public-surface / no-split] | [barrel/private] | [allowed imports] | [test files] | [why] |

## Shared Preflight (once, main agent)
- Runtime: [what must be healthy — e.g. `pnpm i` done, playground buildable]
- Verify baseline: `pnpm typecheck && pnpm test && pnpm lint` green before start

## Work Units

### WU-1: [title]
- owner_role: [subagent | main-agent]
- packages: [@facet/...]
- objective: [one sentence]
- files (max 5):
  - `path` — [add|update|delete] — [why]
- module_shape: [preserve | sibling-helper | role-directory | package-shared | public-surface | no-split] — [rationale; include public/private boundary, import direction, and test placement when splitting or adding scaffold]
- depends_on: []
- parallel_group: [serial-a | par-1]
- red_check: `[vitest command that FAILS pre-impl, PASSES post-impl]` (target: `[test file]`) — or `N/A: [justification]`
- quick_checks: [scoped commands; first re-runs red_check]
- no_regression_checks: [broader suite]
- test_plan:
  - { type: unit, target: `[test file]`, covers_dc: [DC-001], action: new }
- handoff_format: [changed_files, executed_commands, red_check_output_before, red_check_output_after, green_diff_summary, refactor_decision, pass_fail, next_action]
- DoD:
  - [ ] RED→GREEN evidence reported
  - [ ] [condition]
  - [ ] No regression: [suite]

## Execution Order
- [e.g. WU-1 → (WU-2 ∥ WU-3) → WU-4]; parallel only where file sets are disjoint.

## Final Gate Chain (main agent)
1. `/worktree-prep` creates the isolated `feat/<slug>` worktree before `/implement`.
2. `/update-tests`
3. `/verify`
4. `/code-review` (PASS = P0–P2 = 0)
5. `/live-test`
6. `/update-docs`
7. Squash → one feature commit; commit/PR on the user's explicit go.
- final_gate_owner: main-agent
