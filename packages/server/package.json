{
  "name": "@facet/server",
  "version": "0.0.0",
  "description": "Facet reference transport — a tiny Node SSE + POST server that carries events to the runtime and streams patches back.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@facet/core": "workspace:*",
    "@facet/runtime": "workspace:*"
  }
}
