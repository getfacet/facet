{
  "name": "@facet/agent",
  "version": "0.0.0",
  "description": "Facet agent authoring — the Stage control API ('CLI') an agent uses to compose and mutate its page.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@facet/core": "workspace:*"
  }
}
