{
  "name": "@facet/agent-client",
  "version": "0.0.0",
  "description": "Facet agent-side SDK — dial into a Facet server, receive visitor events, and drive the stage from an external agent (with heartbeat + reconnect).",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@facet/core": "workspace:*"
  }
}
