{
  "name": "@facet/kit",
  "version": "0.0.0",
  "description": "Optional presets — card/hero/grid/… that compose the four low-level bricks. One call for a common shape, raw bricks when you need something custom.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@facet/core": "workspace:*"
  }
}
