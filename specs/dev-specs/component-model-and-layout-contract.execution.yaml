# Execution Manifest: Component Model + Renderer Layout Contract
#
# Canonical completed summary. The feature shipped on this branch; the
# compatibility Work-Unit detail this manifest previously carried is obsolete
# and was replaced by this machine-readable summary. The authoritative spec for
# the current composition contract is composition-canonicalization.

feature_slug: "component-model-and-layout-contract"
status: "completed"
spec_path: "specs/dev-specs/component-model-and-layout-contract.md"
context_evidence_path: "specs/context/component-model-and-layout-contract.md"

superseded_by:
  slug: "composition-canonicalization"
  spec_path: "specs/dev-specs/composition-canonicalization.md"
  manifest_path: "specs/dev-specs/composition-canonicalization.execution.yaml"
  note: >-
    The legacy-alias compatibility layer this feature shipped (dual catalog
    fields, legacy expansion API/tool/SDK names) was removed pre-release. One
    canonical composition contract remains: core validator + expandComposition,
    required catalog.compositions, DEFAULT_COMPOSITIONS, *.composition.json
    file assets, the use_composition tool, Stage.useComposition, and the
    Postgres compositions JSONB column.

preserved_decisions:
  intrinsic_criteria:
    - "generic UI noun"
    - "closed schema"
    - "renderer-owned value"
    - "invariant-safe"
    - "stable vocabulary"
  locked_intrinsic_set:
    - "button"
    - "section"
    - "card"
    - "divider"
    - "tabs"
    - "nav"
    - "form"
    - "search"
    - "filterBar"
    - "table"
    - "chart"
    - "list"
    - "keyValue"
    - "metric"
    - "progress"
    - "badge"
    - "alert"
    - "emptyState"
    - "loading"
  deferred:
    - "modal"
    - "drawer"
    - "menu"
    - "timeline"
    - "steps"
    - "calendar"
    - "kanban"
    - "appShell"
  layout_contract:
    - "parent owns immediate-child placement"
    - "child owns internal layout within parent bounds"
    - "renderer owns clipping/bounds and containment on every root"
    - "box-sizing, min-width 0, max-width 100%, wrapping; no absolute/fixed positioning"
    - "overflow only inside explicit renderer-owned scroll regions"
    - "enforced by private packages/core/react/src/layout-contract.ts + hostile-content tests"
  composition_boundary:
    - "operator/tenant definitions reference only allowed primitives/components"
    - "no raw HTML/JS/CSS, scalar styles, positioning escapes, or client fetch/data binding"
    - "validated at asset load; expanded server/runtime-side into ordinary validated nodes"
    - "theme recipes stay style-only; no hidden default structural library in @facet/assets"
  vocabulary:
    - "agent-facing model: Primitive Brick -> Component -> Catalog"
    - "primitives box/text/media/field are the permanent fallback"
    - "metric is the canonical KPI noun; stat remains a legacy node alias"

completed_work_units:
  - id: "WU-1"
    title: "Core Intrinsic Vocabulary And Renderability"
    outcome: "Intrinsic node types, MetricNode with legacy stat alias, renderable-content detection, private component-validation sibling + tests."
  - id: "WU-2"
    title: "Core Validation And Composition Definitions"
    outcome: "Component nodes integrated into validate.ts; public composition-definition validator distinct from theme recipes; hostile-input tests."
  - id: "WU-3"
    title: "Catalog And Theme Component Surface"
    outcome: "Component-facing catalog shape, default intrinsic catalog, and theme recipe coverage for the intrinsic set."
  - id: "WU-4"
    title: "Runtime Composition Asset Loading"
    outcome: "Composition definitions load as validated immutable assets through the single loadAssets gate."
  - id: "WU-5"
    title: "React Intrinsic Rendering And Layout Contract"
    outcome: "Renderers for all new intrinsics; private layout-contract.ts containment helpers; component-layout hostile-content tests."
  - id: "WU-6"
    title: "Default Assets"
    outcome: "Default style recipes for the intrinsic set preferring metric; no hidden structural component library."
  - id: "WU-7"
    title: "Core Stage Spec And Tool Schemas"
    outcome: "STAGE_SPEC and agent-tools schemas teach the component model, locked intrinsics, composition boundary, and layout contract."
  - id: "WU-8"
    title: "Agent Prompt Consumers And Quickstart Copy"
    outcome: "Prompt kit, reference-agent, quickstart, and bridge copy migrated to component-first, primitive-fallback language."
  - id: "WU-8a"
    title: "Reference Agent Stage Summary Components"
    outcome: "Stage summaries cover the intrinsic component set with bounded output."
  - id: "WU-9"
    title: "Documentation And Migration Notes"
    outcome: "Intrinsic criteria, component/composition boundary, and renderer layout contract documented in canonical docs."

final_gate:
  owner: "main-agent"
  result: "feature hard gate passed on this branch prior to canonicalization"
