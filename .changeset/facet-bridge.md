---
"@facet/bridge": minor
---

Add `@facet/bridge` — a local bridge (`facet-bridge`) that lets a local coding
agent (Claude Code, Codex, …) own a Facet link and drive the page via the `facet`
CLI. Configurable server URL, agent id, method (oneshot/session), and brain
command.
